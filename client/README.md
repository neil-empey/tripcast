TripCast front-end. An application to recieve driving directions and weather conditions along your route. Api(backend) travel_weather_api repo.

In the project directory, you can run:

npm run start;
